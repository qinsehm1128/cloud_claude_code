# Claude Code Container Platform - Environment Configuration
# Claude Code 容器平台 - 环境配置
# Copy this file to .env and modify as needed
# 复制此文件为 .env 并根据需要修改

# ===========================================
# Application Settings / 应用设置
# ===========================================

# Environment: development or production
# 运行环境: development(开发) 或 production(生产)
ENVIRONMENT=development

# Server port
# 服务端口
PORT=8080

# ===========================================
# Database Settings / 数据库设置
# ===========================================

# SQLite database file path
# SQLite 数据库文件路径
DATABASE_PATH=./data/cc-platform.db

# Data directory for storing repositories and other files
# 数据目录，用于存储仓库和其他文件
DATA_DIR=./data

# ===========================================
# Security Settings / 安全设置
# ===========================================

# JWT secret key for authentication (auto-generated if not set)
# JWT 认证密钥（未设置时自动生成）
# Generate with / 生成命令: openssl rand -hex 32
JWT_SECRET=

# Encryption key for sensitive data (auto-generated if not set)
# 敏感数据加密密钥（未设置时自动生成）
# Generate with / 生成命令: openssl rand -hex 32
ENCRYPTION_KEY=

# ===========================================
# Admin Credentials / 管理员凭据
# ===========================================

# Admin username (default: admin)
# 管理员用户名（默认: admin）
ADMIN_USERNAME=admin

# Admin password (auto-generated and shown in logs if not set)
# 管理员密码（未设置时自动生成并显示在日志中）
# Set this for production! / 生产环境请务必设置！
ADMIN_PASSWORD=[REDACTED]

# ===========================================
# Traefik Proxy Settings / Traefik 代理设置
# ===========================================

# Auto-start Traefik container on server startup
# 服务启动时自动启动 Traefik 容器
# Set to true to automatically create and start Traefik if not running
# 设置为 true 时，如果 Traefik 未运行则自动创建并启动
AUTO_START_TRAEFIK=false

# Traefik ports will be auto-assigned from high port range if not specified
# 如果未指定，Traefik 端口将从高端口范围自动分配
# Leave empty or 0 for auto-assignment
# 留空或设为 0 表示自动分配
# TRAEFIK_HTTP_PORT=38080
# TRAEFIK_DASHBOARD_PORT=38081

# Traefik direct port range for container services
# Traefik 直接端口范围，用于容器服务访问
# Default: 30001-30020 (20 ports)
# 默认: 30001-30020（20个端口）
TRAEFIK_PORT_RANGE_START=30001
TRAEFIK_PORT_RANGE_END=30020

# ===========================================
# Optional: Pre-configured API Keys / 可选: 预配置的 API 密钥
# These can also be set via the web UI
# 这些也可以通过 Web 界面设置
# ===========================================

# GitHub Personal Access Token (optional, can be set via UI)
# GitHub 个人访问令牌（可选，可通过界面设置）
# GITHUB_TOKEN=ghp_xxxxxxxxxxxx

# Claude/Anthropic API Key (optional, can be set via UI)
# Claude/Anthropic API 密钥（可选，可通过界面设置）
# ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxx

# Custom Anthropic API URL (optional, for proxies or alternative providers)
# 自定义 Anthropic API 地址（可选，用于代理或其他提供商）
# ANTHROPIC_BASE_URL=https://api.anthropic.com
