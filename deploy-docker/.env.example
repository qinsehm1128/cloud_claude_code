# ============================================
# Docker Deployment Environment Configuration
# Docker 部署环境配置
# ============================================
#
# Copy this file to .env and modify as needed
# 复制此文件为 .env 并根据需要修改
#
# Quick start / 快速开始:
#   cp .env.example .env
#   # Generate keys / 生成密钥:
#   openssl rand -hex 32  # For JWT_SECRET
#   openssl rand -hex 32  # For ENCRYPTION_KEY
#   # Edit .env with your settings
#   ./start.sh
#
# ============================================

# ===========================================
# Port Configuration / 端口配置
# ===========================================

# Frontend exposed port (default: 80)
# 前端暴露端口（默认: 80）
# This is the port where you access the platform
# 这是访问平台的端口
FRONTEND_PORT=80

# ===========================================
# Environment / 运行环境
# ===========================================

# Environment: production or development
# 运行环境: production(生产) 或 development(开发)
ENVIRONMENT=production

# ===========================================
# Security Settings / 安全设置 (REQUIRED / 必填)
# ===========================================

# JWT secret key for authentication (REQUIRED)
# JWT 认证密钥（必填）
# Generate with / 生成命令: openssl rand -hex 32
JWT_SECRET=your_jwt_secret_here_change_this

# Encryption key for sensitive data (REQUIRED)
# 敏感数据加密密钥（必填）
# Generate with / 生成命令: openssl rand -hex 32
ENCRYPTION_KEY=your_encryption_key_here_change_this

# ===========================================
# Admin Credentials / 管理员凭据 (REQUIRED / 必填)
# ===========================================

# Admin username (default: admin)
# 管理员用户名（默认: admin）
ADMIN_USERNAME=admin

# Admin password (REQUIRED - MUST be set for production!)
# 管理员密码（必填 - 生产环境必须设置！）
ADMIN_PASSWORD=your_secure_password_here

# ===========================================
# CORS Settings / CORS 设置
# ===========================================

# Allowed CORS origins (comma-separated)
# 允许的 CORS 来源（逗号分隔）
# Example / 示例: https://myapp.example.com,https://admin.example.com
# Leave empty to allow all origins (not recommended for production)
# 留空允许所有来源（不推荐在生产环境使用）
ALLOWED_ORIGINS=

# ===========================================
# Traefik Settings / Traefik 设置
# ===========================================
#
# Traefik is used for routing to code-server instances
# Traefik 用于路由到 code-server 实例
#

# Auto-start Traefik container on backend startup
# 后端启动时自动启动 Traefik 容器
# Set to true if you want to use code-server subdomain access
# 如果要使用 code-server 子域名访问，设置为 true
AUTO_START_TRAEFIK=false

# Traefik HTTP port (dynamically allocated from this range)
# Traefik HTTP 端口（从此范围动态分配）
# Default range: 38000-39000
# TRAEFIK_HTTP_PORT=38080

# Traefik direct port range for container services
# Traefik 直接端口范围，用于容器服务访问
# These ports can be used to access container services directly
# 这些端口可用于直接访问容器服务
TRAEFIK_PORT_RANGE_START=30001
TRAEFIK_PORT_RANGE_END=30020

# ===========================================
# Domain Settings / 域名设置 (For Production / 生产环境)
# ===========================================
#
# These settings are needed for production deployment
# with domain name access and code-server subdomains
#
# 这些设置用于生产环境部署，支持域名访问和 code-server 子域名
#

# Base domain for code-server subdomain routing
# Code-server 子域名路由的基础域名
# Example / 示例: code.example.com
# Users will access code-server at: {container-name}.code.example.com
# 用户通过以下地址访问 code-server: {容器名}.code.example.com
#
# Required DNS setup / 需要的 DNS 设置:
#   A record (wildcard): *.code.example.com -> Server IP
#
CODE_SERVER_BASE_DOMAIN=

# ===========================================
# Optional: API Keys / 可选: API 密钥
# ===========================================

# GitHub Personal Access Token
# GitHub 个人访问令牌
# Used for: Listing repositories, cloning private repos
# 用于：列出仓库、克隆私有仓库
# Create at: https://github.com/settings/tokens
# GITHUB_TOKEN=ghp_xxxxxxxxxxxx

# Claude/Anthropic API Key
# Claude/Anthropic API 密钥
# Used for: Claude Code CLI in containers
# 用于：容器中的 Claude Code CLI
# Get at: https://console.anthropic.com/
# ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxx

# Custom Anthropic API URL (for proxies or alternative providers)
# 自定义 Anthropic API 地址（用于代理或其他提供商）
# ANTHROPIC_BASE_URL=https://api.anthropic.com

# ===========================================
# Advanced Settings / 高级设置
# ===========================================

# Database path (inside container)
# 数据库路径（容器内）
# Default: /app/data/cc-platform.db
# DATABASE_PATH=/app/data/cc-platform.db

# Data directory (inside container)
# 数据目录（容器内）
# Default: /app/data
# DATA_DIR=/app/data
