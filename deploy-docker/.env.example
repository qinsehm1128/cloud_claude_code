# ============================================
# Docker Deployment Environment Configuration
# Docker 部署环境配置
# ============================================
#
# Copy this file to .env and modify as needed
# 复制此文件为 .env 并根据需要修改
#
# Usage / 使用方法:
#   cp .env.example .env
#   # Edit .env with your settings
#   docker compose up -d
#
# ============================================

# ===========================================
# Port Configuration / 端口配置
# ===========================================

# Frontend exposed port (default: 80)
# 前端暴露端口（默认: 80）
FRONTEND_PORT=80

# ===========================================
# Environment / 运行环境
# ===========================================

# Environment: production or development
# 运行环境: production(生产) 或 development(开发)
ENVIRONMENT=production

# ===========================================
# Security Settings / 安全设置 (REQUIRED / 必填)
# ===========================================

# JWT secret key for authentication
# JWT 认证密钥
# Generate with / 生成命令: openssl rand -hex 32
JWT_SECRET=your_jwt_secret_here_change_this

# Encryption key for sensitive data
# 敏感数据加密密钥
# Generate with / 生成命令: openssl rand -hex 32
ENCRYPTION_KEY=your_encryption_key_here_change_this

# ===========================================
# Admin Credentials / 管理员凭据 (REQUIRED / 必填)
# ===========================================

# Admin username (default: admin)
# 管理员用户名（默认: admin）
ADMIN_USERNAME=admin

# Admin password (MUST be set for production!)
# 管理员密码（生产环境必须设置！）
ADMIN_PASSWORD=your_secure_password_here

# ===========================================
# CORS Settings / CORS 设置
# ===========================================

# Allowed CORS origins (comma-separated)
# 允许的 CORS 来源（逗号分隔）
# Example / 示例: https://myapp.example.com,https://admin.example.com
# Leave empty to allow all origins (not recommended for production)
# 留空允许所有来源（不推荐在生产环境使用）
ALLOWED_ORIGINS=

# ===========================================
# Traefik Settings / Traefik 设置
# ===========================================

# Auto-start Traefik container on server startup
# 服务启动时自动启动 Traefik 容器
AUTO_START_TRAEFIK=false

# Traefik direct port range for container services
# Traefik 直接端口范围，用于容器服务访问
TRAEFIK_PORT_RANGE_START=30001
TRAEFIK_PORT_RANGE_END=30020

# ===========================================
# Optional: API Keys / 可选: API 密钥
# ===========================================

# GitHub Personal Access Token
# GitHub 个人访问令牌
# GITHUB_TOKEN=ghp_xxxxxxxxxxxx

# Claude/Anthropic API Key
# Claude/Anthropic API 密钥
# ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxx

# Custom Anthropic API URL (for proxies or alternative providers)
# 自定义 Anthropic API 地址（用于代理或其他提供商）
# ANTHROPIC_BASE_URL=https://api.anthropic.com

# ===========================================
# Optional: Code-Server Settings / 可选: Code-Server 设置
# ===========================================

# Base domain for code-server subdomain routing
# Code-server 子域名路由的基础域名
# Example / 示例: code.example.com
# CODE_SERVER_BASE_DOMAIN=
