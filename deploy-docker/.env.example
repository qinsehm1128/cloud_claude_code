# ============================================
# Docker 部署环境配置 / Docker Deployment Configuration
# ============================================
#
# 使用方法 / Usage:
#   1. cp .env.example .env
#   2. 修改下面的必填项 / Edit required fields below
#   3. ./start.sh
#
# ============================================

# ===========================================
# 必填配置 / REQUIRED SETTINGS
# ===========================================

# 管理员密码（必须修改！）
# Admin password (MUST change!)
ADMIN_PASSWORD=change_me_to_secure_password

# JWT 密钥（运行 start.sh 会自动生成）
# JWT secret (auto-generated by start.sh)
JWT_SECRET=

# 加密密钥（运行 start.sh 会自动生成）
# Encryption key (auto-generated by start.sh)
ENCRYPTION_KEY=

# ===========================================
# 域名配置 / DOMAIN SETTINGS
# ===========================================

# 主站点域名（用于 nginx 配置）
# Main site domain (for nginx config)
# 示例 / Example: cc.example.com
DOMAIN=

# Code-Server 子域名基础域名
# Code-server subdomain base
# 示例 / Example: code.example.com
# 用户访问: {container}.code.example.com
CODE_SERVER_BASE_DOMAIN=

# ===========================================
# 端口配置 / PORT SETTINGS
# ===========================================
# 这些端口供宿主机 nginx 代理使用
# These ports are for host nginx to proxy

# 主应用端口（前端容器）
# Main app port (frontend container)
APP_PORT=8080

# Traefik HTTP 端口（用于 code-server 代理）
# Traefik HTTP port (for code-server proxy)
TRAEFIK_HTTP_PORT=8081

# Traefik Dashboard 端口
# Traefik dashboard port
TRAEFIK_DASHBOARD_PORT=8082

# 容器直接访问端口范围
# Container direct access port range
TRAEFIK_PORT_RANGE_START=30001
TRAEFIK_PORT_RANGE_END=30020

# ===========================================
# 可选配置 / OPTIONAL SETTINGS
# ===========================================

# 运行环境 / Environment
ENVIRONMENT=production

# 管理员用户名 / Admin username
ADMIN_USERNAME=admin

# 自动启动 Traefik（code-server 功能需要）
# Auto-start Traefik (required for code-server)
AUTO_START_TRAEFIK=true

# CORS 允许的来源（逗号分隔）
# Allowed CORS origins (comma-separated)
# 示例 / Example: https://cc.example.com
ALLOWED_ORIGINS=

# ===========================================
# API 密钥 / API KEYS (可选 / Optional)
# ===========================================

# GitHub Token（用于仓库管理）
# GITHUB_TOKEN=ghp_xxxx

# Anthropic API Key（用于 Claude Code）
# ANTHROPIC_API_KEY=sk-ant-xxxx

# 自定义 Anthropic API 地址
# ANTHROPIC_BASE_URL=
